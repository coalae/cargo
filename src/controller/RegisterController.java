package controller;

public class RegisterController {

	// neuen User (Admin oder Kunde registrieren)
	/*
	 * Berechtigt dafuer: ADMIN und KUNDE
	 * Moegliche Aktivitaeten:
	 * 1. Fuer Admin oder Kunde neuen Account anlegen
	 * 2. Nach Registrierung (Create Account) auf Login weiterleiten
	 */
}
